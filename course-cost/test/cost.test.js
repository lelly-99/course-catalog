//add a description of what the function should do

describe("courseCost function", function() {

    it("should be able to return ", function(){
        //create variables storing and one with code
        let dateOne = "2023-05-21"
        let codeOne = "r1"
        //create a object to test
        results =  {
            status : "success",
            code: "r1",
            description : course.courseName,
            cost : course.cost,
            discount :  
            amountDue : 
        }
        assert.equal(results, tutorWageCalculator(dateOne, codeOne))
})

